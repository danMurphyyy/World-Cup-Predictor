# Screenshots

Drop three PNGs here, then uncomment the grid in the root `README.md`:

- `graph.png` — the relationship graph (try knockout mode with a team hovered)
- `simulator.png` — the Simulator podium + title-odds list
- `head-to-head.png` — the Compare view with a marquee fixture

Tip: grab them from the live app at
https://wc26-predictor-eyat.onrender.com (wake it first, then screenshot at a
wide window so the layout is the desktop version).
